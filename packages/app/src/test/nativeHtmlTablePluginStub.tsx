/**
 * Stub for `@native-html/table-plugin` in render tests (TER-461).
 *
 * Flow-typed deep imports (esbuild "Unexpected token 'typeof'"). Imported by
 * MarkdownContent for the native table renderer (unused on web, but loaded at
 * module init). Aliased by package name in vitest.config.ts.
 */
const TableRenderer = () => null
export const tableModel = {}

export default TableRenderer
