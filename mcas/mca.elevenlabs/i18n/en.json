{
  "tools": {
    "-health-check": {
      "name": "-health-check",
      "description": "Internal health check tool. Verifies API key and connectivity to ElevenLabs."
    },
    "text-to-speech": {
      "name": "text-to-speech",
      "description": "Generate speech from text using ElevenLabs. Returns the audio file path. Use the default voice (do not specify voiceId) unless the user explicitly requests a different voice.",
      "params": {
        "text": "The text to convert to speech",
        "voiceId": "Voice ID to use. Use list_voices to see available voices. Default: Marianne Muller (Spanish)",
        "modelId": "Model to use: eleven_monolingual_v1, eleven_multilingual_v1, eleven_multilingual_v2, eleven_turbo_v2, eleven_turbo_v2_5, eleven_flash_v2_5 (default)",
        "stability": "Voice stability (0.0 to 1.0, default: 0.5). Higher = more stable/consistent",
        "similarityBoost": "Similarity boost (0.0 to 1.0, default: 0.75). Higher = closer to original voice",
        "style": "Style exaggeration (0.0 to 1.0, default: 0). Higher = more expressive",
        "speakerBoost": "Enable speaker boost for better quality (default: true)",
        "outputFormat": "Output format: mp3_44100_128, mp3_44100_192, pcm_16000, pcm_22050, pcm_24000, pcm_44100",
        "outputPath": "Custom output path for the audio file (optional). Defaults to /workspace/tts-{timestamp}.mp3"
      }
    },
    "list-voices": {
      "name": "list-voices",
      "description": "List available voices from ElevenLabs account. Supports filtering by name, category, and limiting results.",
      "params": {
        "search": "Search term to filter voices by name (case-insensitive, optional)",
        "limit": "Maximum number of voices to return (optional, default: all)",
        "category": "Filter by voice category: premade, cloned, generated, professional (optional)"
      }
    },
    "get-voice": {
      "name": "get-voice",
      "description": "Get detailed information about a specific voice including settings and samples.",
      "params": {
        "voiceId": "The ID of the voice to get information about"
      }
    },
    "generate-conversation": {
      "name": "generate-conversation",
      "description": "Generate a multi-speaker conversation from a YAML script. Reads a YAML file with speaker voices and dialogue, generates audio for each line, and combines them into a single audio file. Requires ffmpeg installed.",
      "params": {
        "scriptPath": "Path to the YAML script file (absolute or relative to current directory)"
      }
    },
    "get-subscription": {
      "name": "get-subscription",
      "description": "Get information about the current ElevenLabs subscription including character usage and limits."
    }
  },
  "name": "ElevenLabs",
  "description": "ElevenLabs Text-to-Speech - generate natural-sounding speech from text using AI voices",
  "changelog": [
    {
      "notes": "Initial release — turn text into natural speech, browse and inspect AI voices, build multi-speaker conversations, and track plan usage"
    }
  ]
}
