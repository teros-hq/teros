# Higgsfield API credentials
# Get your credentials from https://cloud.higgsfield.ai

# Option 1: Combined key (key:secret format)
HF_KEY=your-api-key:your-api-secret

# Option 2: Separate key and secret
API_KEY=your-api-key
API_SECRET=your-api-secret
