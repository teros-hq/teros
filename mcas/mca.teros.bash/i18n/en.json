{
  "tools": {
    "bash": {
      "name": "bash",
      "description": "Execute a bash command with timeout and working directory support. Returns stdout, stderr, and exit code.",
      "params": {
        "command": "The bash command to execute",
        "description": "A 5-10 word description of what this command does",
        "timeout": "Timeout in milliseconds (default: 120000 / 2 minutes, max: 600000 / 10 minutes)",
        "cwd": "Working directory to execute the command in (optional)"
      }
    }
  },
  "name": "Bash",
  "description": "Execute bash commands on the system. Provides shell access for automation and system tasks.",
  "changelog": [
    {
      "notes": "Initial release — run shell commands on the system, set a working directory and timeout, and capture the output"
    }
  ]
}
